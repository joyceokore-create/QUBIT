// Vitest stub for Next.js's `server-only` marker package, which has no resolvable
// entry outside the Next bundler. Server modules import it as a client-bundling
// guard; under test it's a no-op.
export {};
