# Flat node_modules layout (npm-style) so Prisma's generated client lands at
# node_modules/.prisma/client at the top level — required by the Docker build's
# `COPY node_modules/.prisma` step (pnpm's default symlinked layout hides it in .pnpm).
node-linker=hoisted
